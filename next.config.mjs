/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.dusdusan.com'], // Allow images from this domain
  },
};

export default nextConfig;
